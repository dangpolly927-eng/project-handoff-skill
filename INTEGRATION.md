# 自动交接接入规则

这个文件定义连接工具如何调用 `project-handoff-continuity`。

## 放置位置

流程固定为：

用户 / ChatGPT
→ 上下文判断层
→ project-handoff-continuity Skill
→ 保存交接状态
→ 新会话读取交接状态
→ 继续原计划

## 判断层只负责一件事

判断“现在是否该交接”。

它不负责自己总结项目，也不负责自己生成交接内容。

一旦判断需要交接，必须调用 `project-handoff-continuity`，由 Skill 按固定格式生成和保存交接。

## 触发条件

满足任意一条即可触发：

1. Harness 能返回上下文占用比例，并达到预设阈值。
2. 当前阶段已经完成，适合主动换会话。
3. 当前任务已经形成稳定检查点，继续在旧会话里做收益不高。
4. 系统发现上下文开始丢失早期约束、重复提问、方向漂移或明显遗忘。
5. 用户主动要求“交接”“换窗口”“开新会话”。
6. 平台返回即将达到或已经达到会话限制的明确信号。

## 默认策略

不要依赖反复压缩上下文。

优先策略是：

- 提前交接；
- 保存最新状态；
- 开新会话；
- 新会话读取交接后继续。

## 交接前必须完成

判断层触发后，Skill 必须：

1. 检查真实代码 / Git 状态。
2. 检查 MASTER_PLAN。
3. 更新 CURRENT_STATE。
4. 写清楚已经完成什么。
5. 写清楚正在做什么。
6. 写清楚下一步准确动作。
7. 写清楚剩余的大步骤。
8. 写清楚是否需要用户决定。
9. 如果不需要用户决定，新会话必须直接继续，禁止重新询问用户是否继续。
10. 保存最新检查点或 commit（如果项目使用 Git）。

## 新会话启动规则

新会话启动后必须先调用 `project-handoff-continuity` 的恢复流程。

读取顺序：

1. PROJECT_ORIGIN
2. USER_WORK_RULES
3. MASTER_PLAN
4. CURRENT_STATE
5. 最近的 DECISIONS
6. Git / 真实代码状态

然后直接从 CURRENT_STATE 的 `next_action` 开始。

不得重新规划整个项目。
不得要求用户重新讲需求。
不得因为新会话自己的偏好改掉原计划。

## ChatGPT 网页 / App 的限制

如果上层是官方 ChatGPT 网页或 App，并且平台没有提供精确的剩余上下文数值：

- 判断层不能假装知道准确百分比；
- 采用“阶段结束主动交接 + 异常信号提前交接”；
- 用户也可以随时手动触发交接；
- 新 ChatGPT 窗口如果无法被程序自动创建，就由用户新开窗口；交接数据仍由 Skill 自动准备好。

## Harness 的规则

如果 Harness 能提供上下文占用或会话状态：

- 判断层可以使用真实数值触发交接；
- 不需要等到上下文完全撞满；
- 阈值由连接工具配置；
- 达到阈值后先交接，再结束旧 Harness 会话；
- 新 Harness 会话读取交接后继续。

## 目标

用户只负责真正需要产品判断的事情。

交接、进度保存、下一步恢复、Harness 会话切换尽量自动完成。
